<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender with colored output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan(%-40.40logger{39}) : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender: daily rotation, 30-day retention, 500MB cap -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/bot.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/bot.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %-40.40logger{39} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===== Default (prod) log levels ===== -->

    <!-- Application -->
    <logger name="me.golemcore.bot" level="INFO"/>
    <!-- Security events always visible -->
    <logger name="me.golemcore.bot.security" level="INFO"/>
    <!-- MCP lifecycle -->
    <logger name="me.golemcore.bot.adapter.outbound.mcp" level="INFO"/>

    <!-- Third-party -->
    <logger name="dev.langchain4j" level="WARN"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.telegram" level="WARN"/>

    <!-- Root -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- ===== Dev profile overrides ===== -->
    <springProfile name="dev">
        <logger name="me.golemcore.bot" level="DEBUG"/>
        <logger name="dev.langchain4j" level="DEBUG"/>
    </springProfile>

</configuration>
