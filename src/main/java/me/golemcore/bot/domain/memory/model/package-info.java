/**
 * Memory orchestration DTOs.
 *
 * <p>
 * These models carry structured plans and intermediate results between
 * retrieval, disclosure, and persistence steps. They are intentionally simple
 * transport objects and do not own storage or scoring behavior.
 */
package me.golemcore.bot.domain.memory.model;
