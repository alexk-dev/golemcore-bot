/**
 * Memory lifecycle persistence helpers.
 *
 * <p>
 * This package covers extraction, normalization, JSONL persistence, and
 * promotion between memory layers. It preserves Memory V2 storage compatibility
 * and does not decide prompt disclosure policy.
 */
package me.golemcore.bot.domain.memory.persistence;
